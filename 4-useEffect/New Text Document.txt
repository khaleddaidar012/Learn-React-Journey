ايه هو ال side effect
هي مهام مهمه عاوز تنفذها ولاكن لا تؤثر لي دورة عرض المكونات الرئيسيه
مثلا ترتيب العناصر ال ظاهرة مثلا
الرتيب مهم سب العناصر كدا كداظاهره
---------------------------------------------------------------------------
🍔 مثال من الحياة: "الشيف والمطعم"تخيل أن الـ Component هو عبارة عن شيف وظيفته الأساسية هي طبخ الطعام (إرجاع JSX ليظهر على الشاشة).
الوظيفة الأساسية (Pure): الشيف يأخذ المقادير (Props/State) $\leftarrow$ ويخرج طبق طعام (UI). هذه عملية نظيفة ومباشرة.

الـ Side Effect (عمل جانبي): ماذا لو أراد الشيف أثناء الطبخ أن:

يتصل بالمورد لطلب خضار ناقص (Fetching Data).

يغير إضاءة المطبخ (Changing DOM directly).

يضبط منبه للفرن (Setting Timers
هذه الأفعال تسمى Side Effects لأنها تؤثر على العالم الخارجي (خارج وعاء الطبخ) ولا تتعلق بتحويل المقادير لطبق بشكل مباشر.
------------------------------------------------------------------------
💻 ما هي الـ Side Effects في البرمجة؟
في React، الوظيفة الأساسية للـ Component هي الـ Rendering (الرسم على الشاشة). أي شيء آخر يعتبر Side Effect، وأشهر الأمثلة:

جلب البيانات (Fetching Data): الاتصال بـ API لجلب بيانات من السيرفر.

التعامل مع المتصفح (DOM Manipulation): مثل تغيير عنوان الصفحة (document.title).

التوقيت (Timers): استخدام setTimeout أو setInterval.

لكي ننفذ هذه العمليات في React بشكل صحيح، نستخدم أداة (Hook) اسمها useEffect.

----------------------------------------------------
💡 ملخص قواعد useEffect
الـ useEffect تخبر React أن يفعل شيئاً بعد أن يقوم برسم الـ Component. وهي تأخذ معاملين:

الكود الذي تريد تنفيذه (Dala function).

useeffect( () =>{} , [] )

ftrist is the function will excued
second is the Dependency 
مصفوفة الاعتماديات (Dependency Array): وهي تتحكم متى يعمل هذا الكود:

useEffect(() => { ... })) يعمل بعد كل عملية Render <----- (خطير وقد يسبب بطء
 useEffect(() => { ... }, []) )  يعمل مرة واحدة فقط عند بداية ظهور الـ Component مثل .


useEffect(() => { ... }, [variable]) يعمل عند البداية، وأيضاً كلما تغيرت قيمة variable.

--------------------------------------------------------------------------------
\